name: Digit-UI Image Build workflow
on:
  push:
    branches:
      - dpg-qa-deployment
    paths:
      - 'frontend/micro-ui/**'
  pull_request:
    branches:
      - dpg-qa-deployment
    paths:
      - 'frontend/micro-ui/**'
  workflow_dispatch:
  workflow_call:

jobs:
  docker_image-build:
    if: github.ref == 'refs/heads/dpg-qa-deployment' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Setup Docker
        uses: docker/setup-buildx-action@v1

      # Login to Azure Container Registry
      - name: Login to Azure Container Registry
        run: docker login -u pucarsolutionsdev -p ${{ secrets.ACR_PASSWORD }} pucarsolutionsdev.azurecr.io

      - name: Build and Push Docker image for digit-ui
        run: |
          docker build  -t pucarsolutionsdev.azurecr.io/digit-ui:v1.0.0-${{ github.sha }} -f  web/docker/Dockerfile .
          docker push pucarsolutionsdev.azurecr.io/digit-ui:v1.0.0-${{ github.sha }}
        working-directory: frontend/micro-ui

  Workflow_Trigger:
    if: github.ref == 'refs/heads/dpg-qa-deployment' && github.event_name == 'push'
    runs-on: ubuntu-latest
    needs: docker_image-build
    steps:
      - name: Trigger Deployment Pipeline for Solutions Dev
        run: |
          curl -X POST \
          -H "Accept: application/vnd.github.v3+json" \
          -H "Authorization: token ${{ secrets.GHUB_TOKEN }}" \
          https://api.github.com/repos/pucardotorg/pucar-Devops/actions/workflows/solutions-dpg-qa.yaml/dispatches \
          -d '{"ref":"Solutions-Pipeline"}'
