import { useIndividualView } from "./useIndividualView";
import utils from "../utils";
const pucar = {
  useIndividualView
};

const Hooks = {
  pucar
};

const Utils = {
  browser: {
    pucar: () => { },
  },
  pucar: {
    ...utils,
  },
};

export const CustomisedHooks = {
  Hooks,
  Utils,
};
