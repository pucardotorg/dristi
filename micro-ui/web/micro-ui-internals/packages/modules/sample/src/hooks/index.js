import { useIndividualView } from "./useIndividualView";
const sample = {
  useIndividualView
};

const Hooks = {
  
  sample,

};

const Utils = {
  browser: {
    sample: () => { },
  },
  sample: {
    ...utils,
  },
};

export const CustomisedHooks = {
  Hooks,
  UserService,
  Utils,
};
